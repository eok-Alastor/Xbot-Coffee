---
import Header from '../components/Header.astro';
const { title } = Astro.props;
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <!-- Head 部分和你现有的 Layout.astro 完全一样 -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title} | Obubu奥博熊</title>
  <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="shortcut icon" href="/favicon.ico" />
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  <meta name="apple-mobile-web-app-title" content="Obubu" />
  <link rel="manifest" href="/site.webmanifest" />

  <!-- (可选) 引入更像苹果的字体 Inter -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

  <style is:global>
    /* 定义一个隐藏滚动条的工具类 */
    .no-scrollbar::-webkit-scrollbar { display: none; }
    .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }

    /* (可选) 将 Inter 设置为默认字体 */
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<!-- 
  这是一个常规的 body，背景色为苹果官网常用的浅灰色。
  没有 flex, 没有 fixed, 没有 overflow-hidden。
-->
<body class="bg-[#f5f5f7]">
  
  <!-- Header 会被正常渲染，但会随着页面滚动而消失 -->
  <Header />

  <!-- 内容区 -->
  <main>
    <slot />
  </main>

  <!-- 你可以在这里放一个全局的 Footer -->

</body>
</html>