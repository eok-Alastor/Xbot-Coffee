---
// src/components/PartnerSection.astro
---

<section id="partner" class="py-20 bg-gray-800 text-white">
  <div class="container mx-auto px-6">
    <div class="lg:flex items-center gap-16">
      <div class="lg:w-1/2">
        <h2 class="text-4xl font-bold mb-4">成为我们的合伙人</h2>
        <p class="text-gray-300 text-lg mb-8">
          我们为场地提供方和城市合伙人提供清晰、高回报的合作路径。立即联系我们，开启您的智能咖啡事业。
        </p>
      </div>
      <div class="lg:w-1/2 mt-10 lg:mt-0">
        <div
          id="partner-form-container"
          class="bg-white rounded-lg shadow-xl p-8 text-gray-800"
        >
          <h3 class="text-2xl font-bold mb-6 text-center">
            填写信息，即刻洽谈
          </h3>
          <form id="partner-form">
            <div class="mb-4">
              <label for="name" class="block text-gray-700 font-medium mb-2"
                >您的姓名</label
              >
              <input
                type="text"
                id="name"
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-brand-brown"
                required
              />
            </div>
            <div class="mb-4">
              <label for="phone" class="block text-gray-700 font-medium mb-2"
                >联系电话</label
              >
              <input
                type="tel"
                id="phone"
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-brand-brown"
                required
              />
            </div>
            <div class="mb-6">
              <label for="message" class="block text-gray-700 font-medium mb-2"
                >您的需求</label
              >
              <textarea
                id="message"
                rows="4"
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-brand-brown"
                placeholder="例如：我想了解场地合作模式..."></textarea>
            </div>
            <button
              type="submit"
              class="w-full bg-brand-brown text-white font-bold py-3 px-6 rounded-md hover:opacity-90 transition"
              >提交信息</button
            >
          </form>
          <div
            id="feedback-message"
            class="mt-4 text-center text-green-600 font-medium"
          >
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<script>
  const form = document.getElementById("partner-form");
  const feedbackMessage = document.getElementById("feedback-message");
  if (form && feedbackMessage) {
    form.addEventListener("submit", (event) => {
      event.preventDefault();
      feedbackMessage.textContent =
        "信息提交成功！我们的合作顾问将尽快与您联系。";
      form.reset();
      setTimeout(() => {
        feedbackMessage.textContent = "";
      }, 5000);
    });
  }
</script>
